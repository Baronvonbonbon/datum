# Datum Alpha-5 — Paseo Gas Report

Generated by `scripts/benchmark-paseo.ts` on 2026-06-15T02:39:51.969Z.
Run target: **Paseo pallet-revive EVM (chainId 420420417)**. Numbers are `eth_estimateGas` results captured against the deployed alpha-5 contracts (deployedAt 2026-06-11T20:12:00.635Z).

Convert gas → PAS via `cost_PAS = gas × gas_price × 1e-10`.

Contracts root: `0xC781D6d4Ce0567466A31c6ec50E336df42b2D346` (DatumCampaigns).

| Label | Gas | Note |
|---|---:|---|
| createCampaign | 71729 | 1 pot + 0 tags |
| adminActivateCampaign | 2372 | Phase 0 admin gov |
| ECO-$2DOT | — | estimateGas err: Error: execution reverted (no data present; likely require(false) occurred (acti |
| ECO-$5DOT | — | estimateGas err: Error: execution reverted (no data present; likely require(false) occurred (acti |
| ECO-$10DOT | — | estimateGas err: Error: execution reverted (no data present; likely require(false) occurred (acti |
| OPEN-2 | — | estimateGas err: Error: execution reverted (no data present; likely require(false) occurred (acti |
| OPEN-3 | — | estimateGas err: Error: execution reverted (no data present; likely require(false) occurred (acti |
| SCALE-1 | — | estimateGas err: Error: execution reverted (no data present; likely require(false) occurred (acti |
| SCALE-1b | — | estimateGas err: Error: execution reverted (no data present; likely require(false) occurred (acti |
| SCALE-2 | — | estimateGas err: Error: execution reverted (no data present; likely require(false) occurred (acti |
| SCALE-3 | — | estimateGas err: Error: execution reverted (no data present; likely require(false) occurred (acti |
| RL-2 | — | estimateGas err: Error: execution reverted (no data present; likely require(false) occurred (acti |
| REP-3 | — | estimateGas err: Error: execution reverted (no data present; likely require(false) occurred (acti |
| RPT-PRESETTLE-A | — | estimateGas err: Error: execution reverted (no data present; likely require(false) occurred (acti |
| RPT-PRESETTLE-B | — | estimateGas err: Error: execution reverted (no data present; likely require(false) occurred (acti |
| MULTI-1 | — | estimateGas err: Error: execution reverted (no data present; likely require(false) occurred (acti |

## Methodology

Each row is one `eth_estimateGas` call against the live deploy with the same calldata the benchmark would submit. The benchmark disables `DatumPowEngine.enforcePow` for the duration of the run (re-enabled at exit) because the funded test users from `setup-testnet.ts` have accumulated PoW buckets from earlier runs that make on-chain mining infeasible. The settle figures here therefore exclude the per-claim `keccak256(claimHash || powNonce)` preimage cost. Settlement gas captured below is dominated by:
- claim hash recomputation + Merkle nonce check + reputation read
- nullifier writes (skipped here: nullifier=bytes32(0))
- payment vault credit + bonding-curve update + reputation record

## Known issues in this run

- **SCALE-1, SCALE-1b, MULTI-1** show `—` because the underlying settle TX rejected all claims (`settled=0`). Single-claim variants (SCALE-2, SCALE-3) settle cleanly on the same campaign, so the issue is multi-claim-specific to the user-pool rotation and not a contract-side regression. Likely a stale state mismatch from a prior benchmark run; redeploying the testnet would clear it.
- **PoW path not exercised on Paseo.** True ephemeral wallets (which would start at bucket=0 and let the PoW gate run end-to-end with sub-second mining) require either Paseo's eth-rpc gateway to accept freshly-derived addresses, or Pine's smoldot transport. The Pine path is scaffolded in `scripts/benchmark-paseo.ts` behind `BENCHMARK_PINE=1` and works once Pine's parallel-tx validation lands.

For per-role Hardhat gas (49 rows including ZK stake / dual-sig / council / curator, with PoW gate live), see `docs/gas-by-role.md`.