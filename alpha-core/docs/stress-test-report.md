# Alpha-Core — Production Stress / Threshold Report

Generated by `scripts/stress-test.ts` on 2026-06-15T02:32:46.491Z.
Run target: **Hardhat in-process EVM** — block gas limit 1e9 (raised for deploy workaround; not a real ceiling).
Thresholds compared against the **30,000,000-gas Ethereum reference block**; Polkadot Hub per-tx weight is validated separately on Paseo.

---

## 1. Settlement batch scaling (`settleClaims`)

Default `maxBatchSize` = **50** claims; hard ceiling `MAX_BATCH_SIZE_CEILING` = **200**.
Each claim = 100 impressions, single CPM pot. Gas vs the 30M-gas Ethereum block reference:

| Claims/batch | Gas used | Gas/claim | Marginal gas/claim | % of 30M block |
|---:|---:|---:|---:|---:|
| 1 | 560,882 | 560,882 | 560,882 | 1.9% |
| 5 | 512,296 | 102,459 | -12,146 | 1.7% |
| 10 | 670,989 | 67,098 | 31,738 | 2.2% |
| 25 | 1,147,260 | 45,890 | 31,751 | 3.8% |
| 50 | 1,941,117 | 38,822 | 31,754 | 6.5% |
| 75 | 2,735,645 | 36,475 | 31,781 | 9.1% |
| 100 | 3,530,076 | 35,300 | 31,777 | 11.8% |
| 150 | 5,120,469 | 34,136 | 31,807 | 17.1% |
| 200 | 6,712,287 | 33,561 | 31,836 | 22.4% |

**Steady-state fit (warm, from n=150 & n=200):** `gas(n) ≈ 345,015 + 31,836·n` — a fixed per-batch overhead plus ~31,836 gas/claim.
First settle in a fresh run carries ≈ **184,031 gas of one-time cold-storage init** on top of the fit (so the n=1/n=5 marginals above read low).
At the 30M-gas Ethereum reference a single settle tx tops out near **931 claims** — far above the 200-claim contract ceiling, so the protocol cap (E28) binds first on a 30M-gas chain. The Polkadot Hub per-tx weight cap is the real-world ceiling (validated on Paseo below).

**Cap enforcement:**
- maxBatchSize=50, n=51 → reverts `reverted with custom error 'E28()'` (E28 batch-too-large). ✅ cap binds.
- setMaxBatchSize(201) or settle(201) → `E28` (ceiling MAX_BATCH_SIZE_CEILING=200). ✅ ceiling binds.

## 2. `settleClaimsMulti` scaling (users × campaigns per tx)

One claim per (user, campaign) cell, submitted by the publisher's relaySigner in a single tx.

> **Access-control threshold (E32):** `settleClaims`/`settleClaimsMulti` only accept a caller that is the claim's `user` (self-settle), the registered relay contract, the attestation verifier, or the publisher's `relaySigner`. A third party cannot batch-settle *other* users' claims — multi-user settlement is therefore a **relay-gated** operation. Below we submit as the publisher's relaySigner.

| Users | Campaigns/user | Total claims | Gas used | Gas/claim | % of 30M block |
|---:|---:|---:|---:|---:|---:|
| 1 | 1 | 1 | 395,269 | 395,269 | 1.3% |
| 1 | 5 | 5 | 1,349,542 | 269,908 | 4.5% |
| 1 | 10 | 10 | 2,542,414 | 254,241 | 8.5% |
| 3 | 1 | 3 | 1,077,298 | 359,099 | 3.6% |
| 3 | 5 | 15 | 3,781,575 | 252,105 | 12.6% |
| 5 | 5 | 25 | 6,372,484 | 254,899 | 21.2% |
| 5 | 1 | 5 | 1,441,971 | 288,394 | 4.8% |

## 3. Campaign volume — `createCampaign` gas vs registry size

Does per-campaign creation cost drift as the campaign registry grows (storage-slot / mapping growth)?

| Campaign # | createCampaign gas | Δ vs first |
|---:|---:|---:|
| 1 | 438,751 | +0 |
| 50 | 438,751 | +0 |
| 100 | 438,751 | +0 |
| 250 | 438,751 | +0 |
| 500 | 438,751 | +0 |

Created 500 campaigns; registry now holds 575 total.
Flat gas across the range ⇒ O(1) creation (no unbounded per-campaign loop or rehash). ✅

## 4. Governance load — voters per campaign

N independent voters vote `aye` (conviction 0, 0.5 DOT each) on one campaign, then it is evaluated.

| Voter # | vote() gas | Cumulative voters |
|---:|---:|---:|
| 1 | 333,153 | 1 |
| 2 | 189,942 | 2 |
| 5 | 189,942 | 5 |
| 10 | 189,942 | 10 |
| 20 | 189,942 | 20 |
| 40 | 189,942 | 40 |

**vote() gas:** first=333,153, last(#40)=189,942 — ≈ constant ⇒ O(1) per vote ✅.
**evaluateCampaign() after 40 voters:** 74,520 gas — O(1) tally (running aye/nay totals, no per-voter loop) ✅.

## 5. Concurrency & contention

### 5a. Per-block settlement value cap (`maxSettlementPerBlock`, E80)

- cap=0.001 DOT/block, settle 0.02 DOT in one block → `reverted with custom error 'E80()'`. ✅ per-block cap binds.
- Default cap = 0 (disabled). When set, it throttles total DOT settled per block across all relays — the lever against settlement-griefing / fund-drain spikes.

### 5b. Per-campaign daily-cap throttle

- dailyCap=0.01 DOT, submit 3×100-imp claims (0.06 DOT) → `reason string 'E26'`. ✅ daily cap enforced as a **hard revert (E26)** — fail-closed: the whole over-budget batch is rejected, not partially settled.
- The daily cap bounds how fast any one campaign's budget can drain in a window — the throttle against a compromised/buggy relay over-billing a single advertiser. A relay must size batches to the remaining daily allowance.

### 5c. Replay & double-spend model

- First settle of a 3-claim chain → settled=3, rejected=0.
- Re-submitting the identical plain-CPM batch → settled=3, rejected=0.

> **Architecture note (SLIM #2):** the per-claim nonce no longer travels on the claim — it is *derived* on-chain (`_lastNonce[user][campaign][actionType] + 1`). A plain CPM view-claim therefore carries **no per-claim replay binding**: re-submitting bills the budget again until an economic throttle stops it (budget exhaustion, daily cap, `maxSettlementPerBlock`, or the optional rate limiter). Per-impression replay protection for sensitive flows comes from the **proof sidecar**:
>
> - **ZK campaigns** → `nullifier` (DatumNullifierRegistry, reverts `E73` on reuse — see `nullifier-registry.test.ts`).
> - **CPC clicks** → `clickSessionHash` (DatumClickRegistry `markClaimed`, one settle per recorded click).
> - **CPA actions** → `actionSig` (an off-chain verifier EOA signs each action).
>
> Net: the permissionless plain-CPM path trusts the relay/claim-builder for impression uniqueness and relies on **economic caps** as the on-chain backstop; campaigns needing cryptographic anti-replay set an AssuranceLevel that forces the nullifier/click/action sidecar.

